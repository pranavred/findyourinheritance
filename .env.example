# Template of the secrets / env vars the project expects.
# Copy this to `.env` and replace the placeholder values with real ones.
# Never commit `.env` itself — it's already in `.gitignore`.

# Replicate API token (https://replicate.com/account/api-tokens)
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
